[project]
name = "pa2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "missingno>=0.5.2",
]

[tool.uv]
dev-dependencies = [
    "ipykernel>=6.29.5",
    "pip>=24.2",
]
